timestamp_format        long

snapshot_preserve_min   1d
snapshot_preserve       4d 2w 3m

snapshot_dir           snapshots/erik3

lockfile                   /var/lock/zencefil-btrbk.lock

volume {{actual_rootfs_mountpoint}}
  subvolume rootfs

